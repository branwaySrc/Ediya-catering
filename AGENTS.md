# Ediya Catering 작업 가이드

이 문서는 신규 Chat에서 바로 작업을 이어가기 위한 프로젝트 운영 기준입니다.
작업을 시작하기 전에 이 파일을 먼저 읽고, 현재 코드 구조를 한 번 더 확인한 뒤 진행합니다.

## 프로젝트 개요

- 이 프로젝트는 이디야 케이터링, 기프트, 오피스 스낵바 솔루션을 소개하고 상담 전환을 유도하는 Next.js 사이트입니다.
- 주요 도메인은 `catering`, `gift`, `snackbar`입니다.
- 사용자는 재사용 가능한 컴포넌트를 `share/`에 모아두는 방식을 선호합니다.
- 상태 관리는 Zustand를 사용합니다. 현재 `domains/features/catering/store.ts`에서 사용 중입니다.

## 기술 스택

- Next.js App Router
- React
- TypeScript
- Tailwind CSS
- Zustand
- lucide-react

## 폴더 역할

- `app/`: 라우트와 페이지 조립을 담당합니다. 페이지 파일은 가능한 한 도메인 컴포넌트를 import해서 배치하는 수준으로 유지합니다.
- `domains/`: 비즈니스 도메인별 UI, 데이터, 상태를 둡니다.
- `domains/landing/`: 메인 랜딩 페이지의 섹션 컴포넌트입니다.
- `domains/features/catering/`: 케이터링 상세 페이지의 컴포넌트, 데이터, Zustand store입니다.
- `domains/features/snackbar/`: 오피스 스낵바 상세 페이지의 컴포넌트와 데이터입니다.
- `share/`: 여러 페이지나 도메인에서 재사용 가능한 공용 컴포넌트를 둡니다.
- `globals/`: 전역 레이아웃 요소를 둡니다. 예: navigation, footer, shortcut.
- `public/`: 이미지, 비디오, 폰트 등 정적 자산을 둡니다.

## 컴포넌트 배치 기준

- 여러 도메인에서 재사용 가능하면 `share/`에 둡니다.
- 특정 페이지나 특정 비즈니스 도메인에만 쓰이면 `domains/` 아래에 둡니다.
- 모든 페이지에서 반복되는 전역 UI는 `globals/`에 둡니다.
- `app/` 안에서는 복잡한 UI 구현을 피하고, 라우트별 컴포넌트 조립에 집중합니다.
- 버튼, 섹션 컨테이너, 배지, 카드처럼 반복되는 UI는 기존 `share/` 컴포넌트를 먼저 확인하고 재사용합니다.

## 도메인 작성 패턴

새 기능 도메인을 추가할 때는 아래 구조를 우선 고려합니다.

```txt
domains/features/{domain}/
  data.ts
  store.ts
  hero.tsx
  section-name.tsx
```

- `data.ts`: 화면에 표시되는 정적 데이터와 타입을 둡니다.
- `store.ts`: 사용자 선택, 필터, 견적 등 클라이언트 상태가 필요할 때만 둡니다.
- 컴포넌트 파일은 섹션 단위로 작게 나누고, 페이지는 이 섹션들을 조립합니다.

## Zustand 사용 기준

- Zustand store는 필요한 도메인 내부에 둡니다.
- 전역으로 공유할 이유가 분명하지 않으면 도메인 밖으로 빼지 않습니다.
- 컴포넌트에서는 selector 형태로 필요한 상태와 action만 구독합니다.
- 파생 값은 가능하면 컴포넌트 내부에서 계산하거나, 도메인 helper 함수로 분리합니다.
- store action은 선택 상태 변경처럼 사용자 의도가 드러나는 이름을 사용합니다.

## 스타일과 UI 기준

- 기본 폰트는 `Paperlogy`입니다.
- 주요 브랜드 색상은 `#1B2E74`이며 CSS 변수 `--primary`와 Tailwind `text-primary`, `bg-primary`를 우선 사용합니다.
- 섹션 폭과 좌우 여백은 `share/screen-section.tsx`의 `ScreenSection`을 먼저 고려합니다.
- 아이콘이 필요한 버튼이나 안내 요소는 `lucide-react`를 우선 사용합니다.
- 모바일 화면에서 가로 스크롤, 고정 CTA, 긴 한글 텍스트가 겹치지 않는지 확인합니다.
- 마케팅 랜딩이라도 첫 화면은 실제 서비스와 상품 이미지, 비디오 등 구체적인 시각 자산이 보여야 합니다.

## 한글과 인코딩

- 코드에는 한국어 카피가 많이 포함되어 있습니다.
- PowerShell 터미널 출력에서 한글이 깨져 보일 수 있으므로, 단순 터미널 표시만 보고 문자열이 손상됐다고 판단하지 않습니다.
- 한글 카피를 수정할 때는 파일을 UTF-8 기준으로 다루고, 가능하면 브라우저 화면에서 실제 표시를 확인합니다.
- 깨진 텍스트처럼 보이는 기존 문자열을 대량 수정하기 전에는 실제 파일 내용과 브라우저 렌더링을 먼저 확인합니다.

## 작업 순서

1. `rg --files`로 전체 파일 구조를 확인합니다.
2. 관련 라우트의 `app/**/page.tsx`를 확인합니다.
3. 연결된 `domains/` 컴포넌트와 `data.ts`, `store.ts`를 확인합니다.
4. 재사용 가능한 UI가 필요한 경우 `share/`를 먼저 확인합니다.
5. 변경 범위를 좁게 잡고, 기존 패턴을 우선 따릅니다.
6. 변경 후 `npm run lint`를 실행합니다.
7. 화면 변경이면 개발 서버와 브라우저로 주요 반응형 화면을 확인합니다.

## 검증 명령

```bash
npm run lint
npm run build
npm run dev
```

- 최소 검증은 `npm run lint`입니다.
- 라우팅, metadata, 이미지, 서버 컴포넌트 경계 변경이 있으면 `npm run build`까지 확인합니다.
- UI 변경은 `npm run dev`로 실제 화면을 확인합니다.

## 주의할 점

- 사용자가 만든 변경을 임의로 되돌리지 않습니다.
- 불필요한 리팩터링보다 요청 범위에 맞춘 작은 변경을 우선합니다.
- `share/`는 공용 컴포넌트 공간이므로, 특정 도메인 지식이 강하게 들어가는 컴포넌트는 넣지 않습니다.
- 도메인별 카피, 상품 목록, 가격 정책은 가능하면 `data.ts`로 분리합니다.
- 새 패키지를 추가하기 전에는 기존 의존성으로 해결 가능한지 먼저 확인합니다.
- 이미지나 비디오 자산을 바꿀 때는 `public/` 경로와 `next/image` 사용 방식을 함께 확인합니다.

## 현재 구조 요약

- `/`: 랜딩 페이지. `domains/landing/*` 섹션을 조립합니다.
- `/catering`: 케이터링 상세 페이지. 패키지 선택과 예상 견적 CTA가 있으며 Zustand를 사용합니다.
- `/snackbar`: 오피스 스낵바 상세 페이지. 상품, 가격, 배송, FAQ 등 섹션형 구성입니다.
- `/gift`: 기프트 상세 페이지. 현재는 페이지 파일 안에 섹션과 데이터가 함께 있는 편이라, 확장 시 `domains/features/gift/`로 분리하는 것을 우선 고려합니다.
- `/contact`: 상담 또는 문의 페이지입니다.

